springdoc:
  swagger-ui:
    path: /
  default-produces-media-type: application/json

spring:
  liquibase:
    enabled: false
  cloud:
    gcp:
      secretmanager:
        enabled: false

reciplease:
  version: @project.version@