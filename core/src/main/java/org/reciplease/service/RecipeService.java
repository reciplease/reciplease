package org.reciplease.service;

import org.reciplease.model.Recipe;

import java.util.Optional;
import java.util.UUID;

public class RecipeService {
    public Optional<Recipe> get(final UUID uuid) {
        return Optional.empty();
    }
}
