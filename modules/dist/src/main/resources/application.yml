springdoc:
  swagger-ui:
    path: /
  default-produces-media-type: application/json

spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      naming-stategy: org.hibernate.cfg.ImprovedNamingStrategy