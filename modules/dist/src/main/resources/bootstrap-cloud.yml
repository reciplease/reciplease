spring:
  cloud:
    gcp:
      core:
        enabled: true
      secret-manager:
        enabled: true
  datasource:
    url: ${sm://reciplease-database-url}
    username: ${sm://reciplease-database-username}
    password: ${sm://reciplease-database-password}