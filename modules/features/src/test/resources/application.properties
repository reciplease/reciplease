# Embedded MongoDB (flapdoodle) is auto-configured for tests.
# Spring Boot 4 removed its built-in embedded-mongo auto-configuration, so the
# flapdoodle spring4x starter now provides it and requires an explicit version.
# Kept in sync with modules/database/src/test/resources/application.properties.
de.flapdoodle.mongodb.embedded.version=8.0.17

# Spring Boot 4 no longer defaults the Mongo UUID representation.
spring.mongodb.representation.uuid=standard

# Normally filtered in from the Maven build by modules/dist; this module deliberately
# doesn't depend on dist (avoids its Cloud Run/Secret Manager concerns), so SwaggerConfig
# needs it supplied directly.
reciplease.version=test

reciplease.jwt.signing-secret=test-signing-secret-not-for-prod

reciplease.webauthn.rp-id=localhost
reciplease.webauthn.rp-name=Reciplease
reciplease.webauthn.allowed-origins=http://localhost:3000

reciplease.fitbit.client-id=test-fitbit-client-id
reciplease.fitbit.client-secret=test-fitbit-client-secret
reciplease.fitbit.redirect-uri=http://localhost:3000/api/fitbit/callback

